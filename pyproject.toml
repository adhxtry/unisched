[project]
name = "extensible-exam-scheduler"
version = "0.1.0"
description = "An extensible exam scheduler application built with Python, designed to efficiently manage and optimize exam schedules for educational institutions."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.1",
]
